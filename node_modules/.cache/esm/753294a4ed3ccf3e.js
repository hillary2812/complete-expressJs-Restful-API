let sgMail,SENGRID_API,APP_HOST_EMAIL;_ed3‍.x([["default",()=>_ed3‍.o]]);_ed3‍.w("@sendgrid/mail",[["default",["sgMail"],function(v){sgMail=v}]]);_ed3‍.w("../constants",[["SENGRID_API",["SENGRID_API"],function(v){SENGRID_API=v}],["APP_HOST_EMAIL",["APP_HOST_EMAIL"],function(v){APP_HOST_EMAIL=v}]]);


sgMail.setApiKey(SENGRID_API);

const sendMail = async (email, subject, text, html) => {
  try {
    const msg = {
      to: email, // Change to your recipient
      from: APP_HOST_EMAIL, // Change to your verified sender
      subject,
      text,
      html,
    };
    _ed3‍.g.console.log(msg);
    await sgMail.send(msg);
    console.log("MAIL_SENT");
  } catch (err) {
    _ed3‍.g.console.log("ERROR_MAILING", err.message);
  } finally {
    return;
  }
};

_ed3‍.d(sendMail);
