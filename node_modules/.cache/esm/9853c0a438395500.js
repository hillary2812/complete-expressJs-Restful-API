let validationResult;_383‍.x([["default",()=>_383‍.o]]);_383‍.w("express-validator",[["validationResult",["validationResult"],function(v){validationResult=v}]]);

const validationMiddleware = (req, res, next) => {
  let errors = validationResult(req);
  if (!errors.isEmpty()) {
    //if no error then only return json data
    return res.json({
      errors: errors.array(),
    });
  }
  next();
};

_383‍.d(validationMiddleware);
